platform :ios, '16.0'
use_frameworks! :linkage => :static    # <- important: static linking
inhibit_all_warnings!

target 'UniCalendar' do
  pod 'AppAuth', '~> 1.6', :modular_headers => true
  pod 'MSAL', '~> 1.3'
end

